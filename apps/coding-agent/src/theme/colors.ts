import chalk from "chalk";

export const userMessageBg = (s: string) => chalk.bgGray(s);
export const spinnerColor = (s: string) => chalk.cyan(s);
export const mutedColor = (s: string) => chalk.gray(s);
