/**
 * Wire types for the HTTP API. They live in `@nyx/agent/schema` (the services
 * use them too); re-exported here so `@nyx/server/schema` stays the client-
 * facing entry — import it type-only.
 */
export * from "@nyx/agent/schema";
